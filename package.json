{
  "name": "golftracker",
  "version": "1.0.0",
  "private": true,
  "description": "Fast mobile-first LPGA leaderboard app for Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-06-27",
    "deploy": "wrangler pages deploy public",
    "test": "node --check functions/api/leaderboard.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
